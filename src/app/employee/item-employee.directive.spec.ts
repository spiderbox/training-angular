import { ItemEmployeeDirective } from './item-employee.directive';

describe('ItemEmployeeDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemEmployeeDirective();
    expect(directive).toBeTruthy();
  });
});
